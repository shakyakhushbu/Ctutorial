#include<stdio.h>
int main()
{
    int l,b,area;
    printf("Enter the value of length\n");
    scanf("%d",&l);
    printf("Enter the value of breadth\n");
    scanf("%d",&b);
    area=l*b;
    printf("Area of rectangle is:%d",area);
}